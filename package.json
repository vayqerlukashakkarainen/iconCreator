{
  "name": "icon-creator",
  "version": "1.0.0",
  "description": "Convert images to native app icon formats (ICO, ICNS, PNG) - Client-side static version",
  "main": "public/index.html",
  "scripts": {
    "dev": "npx live-server public",
    "build": "node build-static.js",
    "build:pages": "node build-static.js",
    "test": "echo 'No tests specified' && exit 0",
    "lint": "echo 'No linter configured' && exit 0",
    "typecheck": "echo 'No TypeScript configured' && exit 0"
  },
  "dependencies": {
    "icojs": "^0.19.4",
    "jimp": "^0.22.10",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "jest": "^29.7.0",
    "eslint": "^8.52.0",
    "@types/node": "^20.8.7",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}