{
  "name": "icon-creator",
  "version": "1.0.0",
  "description": "Convert images to native app icon formats (ICO, ICNS, PNG)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build step required for this project'",
    "test": "echo 'No tests specified' && exit 0",
    "lint": "echo 'No linter configured' && exit 0",
    "typecheck": "echo 'No TypeScript configured' && exit 0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^2.0.1",
    "sharp": "^0.33.5",
    "png2icons": "^2.0.1",
    "archiver": "^7.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "jest": "^29.7.0",
    "eslint": "^8.52.0",
    "@types/node": "^20.8.7",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}